# ===========================================
# Search List - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values
# Never commit .env.local to version control!

# -------------------------------------------
# Supabase Configuration
# -------------------------------------------
# Get these from your Supabase project settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# -------------------------------------------
# App Configuration
# -------------------------------------------
# The public URL of your application
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Admin email for notifications (new inquiries, reviews, etc.)
ADMIN_EMAIL=admin@example.com

# Email sender address (must be verified in Resend)
EMAIL_FROM=Search List <noreply@yourdomain.com>

# -------------------------------------------
# Resend (Email Service)
# -------------------------------------------
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key

# -------------------------------------------
# Stripe Configuration (Payments)
# -------------------------------------------
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Webhook secret for verifying Stripe events
# Get this when you create a webhook endpoint in Stripe Dashboard
# For local development, use Stripe CLI: stripe listen --forward-to localhost:3000/api/stripe/webhook
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# -------------------------------------------
# Stripe Price IDs (Subscriptions)
# -------------------------------------------
# Create these products/prices in Stripe Dashboard > Products
# Each tier needs monthly and yearly price IDs

# Basic Tier ($29/mo or $290/yr suggested)
STRIPE_PRICE_BASIC_MONTHLY=price_basic_monthly_id
STRIPE_PRICE_BASIC_YEARLY=price_basic_yearly_id

# Featured Tier ($79/mo or $790/yr suggested)
STRIPE_PRICE_FEATURED_MONTHLY=price_featured_monthly_id
STRIPE_PRICE_FEATURED_YEARLY=price_featured_yearly_id

# Premium Tier ($149/mo or $1490/yr suggested)
STRIPE_PRICE_PREMIUM_MONTHLY=price_premium_monthly_id
STRIPE_PRICE_PREMIUM_YEARLY=price_premium_yearly_id

# -------------------------------------------
# Stripe Price IDs (One-time Boosts)
# -------------------------------------------
# Create these as one-time products in Stripe Dashboard

# Spotlight boost ($49 suggested - 7 days featured in search)
STRIPE_PRICE_SPOTLIGHT=price_spotlight_id

# Homepage feature ($99 suggested - 7 days on homepage)
STRIPE_PRICE_HOMEPAGE=price_homepage_id

# Category boost ($79 suggested - 7 days top of category)
STRIPE_PRICE_CATEGORY_BOOST=price_category_boost_id

# -------------------------------------------
# Optional: Analytics & Monitoring
# -------------------------------------------
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
# SENTRY_DSN=https://xxx@sentry.io/xxx
